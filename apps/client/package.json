{
  "name": "@honkoku/client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --bun vite --host 127.0.0.1",
    "build": "bun --bun vite build",
    "check": "bun --bun svelte-check --tsconfig ./tsconfig.json",
    "tauri": "bun x --bun --no-install tauri"
  },
  "dependencies": {
    "@honkoku/client-api": "workspace:*",
    "@honkoku/ui": "workspace:*",
    "svelte": "5.57.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tauri-apps/cli": "2.11.4",
    "svelte-check": "^4.0.0",
    "typescript": "^5.9.0",
    "vite": "8.2.2"
  }
}
