Saved both PNGs and `notes.md` in `design/explorations/c/`, with earlier attempts in `iterations/`. Japanese text was checked; difficult glyphs use readable simplifications.

One step remains: the generator exports 1586×992. May I locally resize both finals to the required 1600×1000?

The image-tool instructions require explicit permission to use another tool for this edit.