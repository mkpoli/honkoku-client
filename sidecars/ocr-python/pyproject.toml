[project]
name = "honkoku-ocr-sidecar"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = ["honkoku-ocr-py[cpu]==0.3.0"]

[project.optional-dependencies]
gpu = ["honkoku-ocr-py[gpu]==0.3.0"]
